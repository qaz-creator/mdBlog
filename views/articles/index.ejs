<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
      integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm"
      crossorigin="anonymous"
    />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />

    <title>Blog</title>
  </head>
  <body>
    <div class="container">
      <h1 class="my-4">Blog</h1>
      <a href="/articles/new" class="btn btn-success">New Article</a>

      <% articles.forEach(article=>{%>
      <div class="card my-4">
        <div class="card-body my-2">
          <h4><%= article.title %></h4>
          <div class="card-subtitle text-mute mb-2">
            <span class="mr-2">
              Created@<%= article.createdAt.toLocaleDateString('en-GB') %>
            </span>
            <span class="ml-2">
              Expected to finish@
              <mark
                ><%= article.expectedDate.toLocaleDateString('en-GB') %></mark
              >
            </span>
          </div>
          <div class="card-text mb-4"><h5><%= article.description %></h5></div>
          <a href="articles/<%= article.slug %> " class="btn btn-primary"
            >Read More</a
          >
          <a href="articles/edit/<%= article.id %> " class="btn btn-info"
            >Edit</a
          >
          <form
            action="/articles/<%= article.id %>?_method=DELETE"
            method="POST"
            class="d-inline"
          >
            <button type="submit" class="btn btn-danger">Delete</button>
          </form>
        </div>
      </div>
      <% }) %>
    </div>
  </body>
</html>
